if (row.Approved === true && row.Approved !== oldRow.Approved) {    var payload = logicContext.transformCurrentRow('AccountingAlert');    logicContext.logDebug("Alert Accounting / API -- payload: " + payload);    resourceURL="http://localhost:8080/rest/default/MktMgmt/v1/ProcessCharges";    // FIXME - outboard    var settings = { headers: { Authorization: "CALiveAPICreator AcctgToken:1" }};    // var reqData = {};  // create payload object for Posting    // reqData.msgContent = payload;    var postResponse = SysUtility.restPost(resourceURL, null, settings, payload);    if (JSON.parse(postResponse).statusCode !== 201) {        throw "Alert Accounting / API -- unexpected post response: " + postResponse;    }}
